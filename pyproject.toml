[project]
name = "lyrics-tools"
version = "0.1.0"
description = "A set of scripts for managing lyrics files in a local music library."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
